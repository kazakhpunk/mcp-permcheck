.DS_Store
.ipynb_checkpoints/
node_modules/
*.log

# Real public MCP servers cloned for analysis demos — upstream repos, not vendored.
# Clone with: git clone --depth 1 --filter=blob:none --sparse https://github.com/modelcontextprotocol/servers.git real-servers/_repo
# Then: cd real-servers/_repo && git sparse-checkout set src/filesystem
real-servers/
*.pdf
.vscode
.docs
.vscode/
